Virtum Mobile WASM placeholder.

The real openslide.js / openslide.wasm / manifest.json are generated by the
GitHub Actions workflow "Build Mobile OpenSlide WASM". Until then the Viewer
falls back to the stock openslide-js WASM automatically.
